---
phase: 05-verification-and-gallery
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - playground/ThemeGallery.qml
  - playground/ThemeGalleryProbe.qml
  - playground/Main.qml
  - playground/main.cpp
  - playground/CMakeLists.txt
autonomous: true
requirements: [VERIFY-02, VERIFY-04]
user_setup: []
must_haves:
  truths:
    - "Playground includes a separate ThemeGallery route integrated from Main.qml."
    - "Gallery shows active brand/mode, palette, typography, spacing/radius, and representative component states."
    - "Offscreen smoke and gallery probe runs prove the gallery loads and representative controls consume active runtime theme values."
  artifacts:
    - path: "playground/ThemeGallery.qml"
      provides: "Verification-oriented theme gallery UI for VERIFY-04"
    - path: "playground/ThemeGalleryProbe.qml"
      provides: "Focused offscreen gallery route/objectName probe"
    - path: "playground/main.cpp"
      provides: "`--theme-gallery-probe` route"
    - path: "playground/CMakeLists.txt"
      provides: "QML registration for ThemeGallery and ThemeGalleryProbe"
  key_links:
    - from: "playground/Main.qml"
      to: "playground/ThemeGallery.qml"
      via: "ThemeGallery child in existing page column"
      pattern: 'ThemeGallery'
    - from: "playground/main.cpp"
      to: "playground/ThemeGalleryProbe.qml"
      via: "`--theme-gallery-probe` module route"
      pattern: 'theme-gallery-probe'
---

<objective>
Build the verification gallery surface and focused gallery probe for VERIFY-02 and VERIFY-04.

Purpose: make runtime theme values and representative Merce component consumption visible in the playground without changing the theme runtime API or turning `Main.qml` into the gallery implementation.
Output: `ThemeGallery.qml`, `ThemeGalleryProbe.qml`, `Main.qml` integration, CMake registration, and a deterministic `--theme-gallery-probe` route.
</objective>

<execution_context>
@/Users/techalp/.codex/get-shit-done/workflows/execute-plan.md
@/Users/techalp/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-verification-and-gallery/05-CONTEXT.md
@.planning/phases/05-verification-and-gallery/05-RESEARCH.md
@.planning/phases/05-verification-and-gallery/05-UI-SPEC.md
@.planning/phases/05-verification-and-gallery/05-PATTERNS.md
@.planning/phases/04-runtime-brand-mode-switching/04-01-SUMMARY.md
@.planning/phases/04-runtime-brand-mode-switching/04-02-SUMMARY.md
@playground/Main.qml
@playground/main.cpp
@playground/CMakeLists.txt
@playground/ThemeSwitchProbe.qml
@Core/Theme/MerceTheme.h
@Controls/MButton.qml
@Foundation/MText.qml
@generated/themes/index.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verification-oriented ThemeGallery</name>
  <files>playground/ThemeGallery.qml</files>
  <read_first>
    - `.planning/phases/05-verification-and-gallery/05-CONTEXT.md` decisions D-04:, D-05:, D-06:, D-07:, D-08:, D-09:, D-13:, D-14:, D-15:.
    - `.planning/phases/05-verification-and-gallery/05-UI-SPEC.md` "Gallery Screen Contract", "Spacing Scale", "Typography", "Color", and "Accessibility And Interaction".
    - `.planning/phases/05-verification-and-gallery/05-PATTERNS.md` `playground/ThemeGallery.qml` assignment.
    - `playground/Main.qml`
    - `Core/Theme/MerceTheme.h`
    - `Controls/MButton.qml`
    - `Foundation/MText.qml`
  </read_first>
  <action>
    Create `playground/ThemeGallery.qml` as a separate component per D-13:. Use the existing playground imports and section-surface pattern. Root objectName must be `merce.playground.gallery`; required child objectNames must be `merce.playground.gallery.activeTheme`, `merce.playground.gallery.palette`, `merce.playground.gallery.typography`, `merce.playground.gallery.spacingRadius`, `merce.playground.gallery.components`, and `merce.playground.gallery.exportStatus` per D-14:. Include a compact active brand/mode readout and selector for `Merce Light`, `Merce Dark`, and `Stripe Reference`; selector calls must be exactly `Theme.setTheme("merce", "light")`, `Theme.setTheme("merce", "dark")`, and `Theme.setTheme("stripe")`, preserving the previous active state when the call returns false. Display Stripe's empty active mode as `default`, not `light`. Use canonical `Theme.palette`, `Theme.iconography`, and `Theme.breakpoints` names in new gallery labels/bindings per D-04:, D-05:, and D-06:. Add palette swatches, typography samples, spacing/radius scale samples, and representative controls from D-08: `MButton` variants, `MText`, `MInput`, `MCheckbox`, `MRadio`, `MSwitch`, `MSelect`, `MToast`, and `MDialog`. Keep the layout compact and verification-oriented per D-15:; do not add marketing/tutorial copy, a broad component-state matrix, generated-token API display, Spix, or Qt Quick Controls style switching.
  </action>
  <acceptance_criteria>
    - `ThemeGallery.qml` exists and its root objectName is `merce.playground.gallery`.
    - Required section objectNames from the UI-SPEC are authored exactly.
    - The gallery includes active brand/mode readout or selector, palette, typography, spacing/radius, component states, and export status.
    - New gallery QML uses canonical `Theme.palette`, `Theme.iconography`, and `Theme.breakpoints`; it does not present `colors`, `icons`, or `breakpoint` as separate token families.
    - Representative controls listed in D-08 appear in the gallery without expanding into a full state matrix.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "objectName: \"merce\\.playground\\.gallery\"|merce\\.playground\\.gallery\\.activeTheme|merce\\.playground\\.gallery\\.palette|merce\\.playground\\.gallery\\.typography|merce\\.playground\\.gallery\\.spacingRadius|merce\\.playground\\.gallery\\.components|merce\\.playground\\.gallery\\.exportStatus" playground/ThemeGallery.qml</automated>
    <automated>rg -n "Theme\\.palette|Theme\\.iconography|Theme\\.breakpoints|Theme\\.setTheme\\(\"merce\", \"light\"\\)|Theme\\.setTheme\\(\"merce\", \"dark\"\\)|Theme\\.setTheme\\(\"stripe\"\\)" playground/ThemeGallery.qml</automated>
    <automated>! rg -n "Theme\\.(colors|icons|breakpoint)\\b" playground/ThemeGallery.qml</automated>
  </verify>
  <done>VERIFY-04 gallery UI exists as a separate route with canonical Theme naming and representative component states.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate gallery and add offscreen gallery probe route</name>
  <files>playground/Main.qml, playground/ThemeGalleryProbe.qml, playground/main.cpp, playground/CMakeLists.txt</files>
  <read_first>
    - `.planning/phases/05-verification-and-gallery/05-CONTEXT.md` decisions D-01:, D-03:, D-07:, D-08:, D-09:, D-13:, D-14:, D-15:.
    - `.planning/phases/05-verification-and-gallery/05-PATTERNS.md` `Main.qml`, `main.cpp`, and `CMakeLists.txt` assignments.
    - `playground/Main.qml`
    - `playground/main.cpp`
    - `playground/CMakeLists.txt`
    - `playground/ThemeSwitchProbe.qml`
    - `playground/ThemeGallery.qml` from Task 1
  </read_first>
  <action>
    Integrate `ThemeGallery { width: parent.width }` from `playground/Main.qml` as a child of the existing `page` column per D-13:. Keep `Main.qml` as a shell with title/subtitle, gallery, and shared notification/dialog support; do not move gallery section implementation into `Main.qml`. Add `playground/ThemeGalleryProbe.qml` as a focused offscreen route that instantiates `ThemeGallery`, verifies all required gallery objectName anchors through an internal readiness property or direct object references, verifies default `Theme.activeBrand === "merce"` and `Theme.activeMode === "light"`, then proves representative component samples consume the active runtime theme per D-09:. The probe must inspect stable gallery-prefixed sample anchors or explicit probe properties for at least one `MButton`, `MText`, `MInput`, one toggle control (`MCheckbox`, `MRadio`, or `MSwitch`), `MSelect`, `MToast`, and `MDialog`; acceptable proof is direct object references, authored `objectName` lookups, or exported `property` values from `ThemeGallery` that expose each sample's observed color/state. Assert the startup sample observations match active `Theme.palette` values such as `backgroundBase`, `backgroundSurface`, `textPrimary`, `actionPrimary`, `borderDefault`, `borderFocus`, or status colors as appropriate for the sampled control. Switch to `Theme.setTheme("merce", "dark")` and assert `Theme.activeBrand === "merce"`, `Theme.activeMode === "dark"`, and the same sample observations either equal the dark `Theme.palette` values or changed from the light observations. Switch to `Theme.setTheme("stripe")` and assert `Theme.activeBrand === "stripe"`, `Theme.activeMode === ""`, and the same sample observations either equal the Stripe `Theme.palette` values or changed from the Merce observations; display-only Stripe mode remains `default`. The probe logs `theme-gallery-probe ok` only after the gallery anchors, representative sample anchors/properties, `merce/dark` assertions, and `stripe` assertions all pass. Update `playground/CMakeLists.txt` QML_FILES with `ThemeGallery.qml` and `ThemeGalleryProbe.qml`. Update `playground/main.cpp` with a concrete `--theme-gallery-probe` flag following the existing `--theme-probe` and `--theme-switch-probe` route pattern. Include `--theme-gallery-probe` in the short auto-quit path. Preserve `QT_QML_DEBUG` for optional qmlagent per D-16:, but do not make qmlagent a required gate per D-18:.
  </action>
  <acceptance_criteria>
    - `Main.qml` integrates `ThemeGallery` and remains a small playground shell rather than a gallery implementation file.
    - `ThemeGalleryProbe.qml` exists and logs `theme-gallery-probe ok` only after required gallery anchors, representative sample anchors/properties, and theme switch states are verified.
    - The gallery/probe exposes stable anchors or explicit observed properties for at least `MButton`, `MText`, `MInput`, one toggle control, `MSelect`, `MToast`, and `MDialog`.
    - Probe assertions after startup, `Theme.setTheme("merce", "dark")`, and `Theme.setTheme("stripe")` prove observed sample colors/states match active `Theme.palette` values or change from the previous active theme.
    - `main.cpp` loads `ThemeGalleryProbe` when `--theme-gallery-probe` is present and keeps existing `--theme-probe`, `--theme-switch-probe`, and `--smoke-test` behavior.
    - `CMakeLists.txt` registers both gallery QML files under `QML_FILES`.
    - Offscreen `--smoke-test` and `--theme-gallery-probe` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build --target MercePlayground</automated>
    <automated>QT_QPA_PLATFORM=offscreen ./build/playground/MercePlayground --smoke-test</automated>
    <automated>QT_QPA_PLATFORM=offscreen ./build/playground/MercePlayground --theme-gallery-probe</automated>
    <automated>rg -n "ThemeGallery|ThemeGalleryProbe|--theme-gallery-probe|theme-gallery-probe ok" playground/Main.qml playground/main.cpp playground/CMakeLists.txt playground/ThemeGalleryProbe.qml</automated>
    <automated>rg -n "MButton|MText|MInput|M(Checkbox|Radio|Switch)|MSelect|MToast|MDialog|Theme\\.setTheme\\(\"merce\", \"dark\"\\)|Theme\\.setTheme\\(\"stripe\"\\)|Theme\\.palette\\.(backgroundBase|backgroundSurface|textPrimary|actionPrimary|borderDefault|borderFocus|status)" playground/ThemeGalleryProbe.qml playground/ThemeGallery.qml</automated>
    <automated>! rg -n "Spix|QQuickStyle|setStyle\\(|qtquickcontrols2\\.conf|Qt6::QuickControls2" playground/Main.qml playground/main.cpp playground/CMakeLists.txt playground/ThemeGallery.qml playground/ThemeGalleryProbe.qml</automated>
  </verify>
  <done>VERIFY-02 and VERIFY-04 have deterministic offscreen gallery smoke/probe proof through the playground, including representative Merce component sample consumption after startup, `merce/dark`, and `stripe` runtime theme states.</done>
</task>

</tasks>

## Artifacts this phase produces

- New QML file `playground/ThemeGallery.qml`.
- New QML file `playground/ThemeGalleryProbe.qml`.
- New CLI flag on `MercePlayground`: `--theme-gallery-probe`.
- Stable gallery objectName contract:
  - `merce.playground.gallery`
  - `merce.playground.gallery.activeTheme`
  - `merce.playground.gallery.palette`
  - `merce.playground.gallery.typography`
  - `merce.playground.gallery.spacingRadius`
  - `merce.playground.gallery.components`
  - `merce.playground.gallery.exportStatus`

<threat_model>
No high-severity threat is expected in this plan because it adds local QML UI and an offscreen probe route only. The qmlagent exposure remains optional and debug-only; mandatory gates stay local.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gallery selector -> Theme.setTheme | User-facing QML selector passes brand/mode strings into the runtime loader. |
| offscreen probe -> QML object tree | Probe inspects authored gallery state and object references. |
| optional qmlagent -> running playground | Debug tooling may inspect authored objectName selectors when explicitly launched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Spoofing | gallery active theme display | mitigate | Read active state from `Theme.activeBrand` and `Theme.activeMode`; display empty Stripe mode as `default` without fabricating a mode. |
| T-05-02-02 | Tampering | `Theme.setTheme` selector input | mitigate | Use fixed selector calls for registered states only: `merce/light`, `merce/dark`, and `stripe`; preserve prior state on false return. |
| T-05-02-03 | Information Disclosure | optional qmlagent object tree | accept | qmlagent is not launched by this plan and remains optional; stable objectNames expose UI structure only. |
| T-05-02-04 | Denial of Service | gallery route/offscreen smoke | mitigate | Keep probe bounded with the existing timer/quit pattern and avoid network or long-running automation. |
| T-05-02-05 | Elevation of Privilege | new dependency surface | mitigate | Do not add Spix, Qt Quick Controls style switching, network calls, or external package installs. |
| T-05-SC | Tampering | package/dependency surface | mitigate | No npm, pip, cargo, Spix, or external package install is planned. |
</threat_model>

<verification>
- `cmake --build build --target MercePlayground`
- `QT_QPA_PLATFORM=offscreen ./build/playground/MercePlayground --smoke-test`
- `QT_QPA_PLATFORM=offscreen ./build/playground/MercePlayground --theme-gallery-probe`
- Source gate: `! rg -n "Theme\\.(colors|icons|breakpoint)\\b" playground/ThemeGallery.qml playground/ThemeGalleryProbe.qml`
- Source gate: `! rg -n "Spix|QQuickStyle|setStyle\\(|qtquickcontrols2\\.conf|Qt6::QuickControls2" playground/Main.qml playground/main.cpp playground/CMakeLists.txt playground/ThemeGallery.qml playground/ThemeGalleryProbe.qml`
</verification>

<success_criteria>
- VERIFY-02 appears in this plan and is proven by offscreen smoke plus gallery probe.
- VERIFY-02 proof includes representative `MButton`, `MText`, `MInput`, toggle, `MSelect`, toast, and dialog sample observations after startup, `Theme.setTheme("merce", "dark")`, and `Theme.setTheme("stripe")`.
- VERIFY-04 appears in this plan and is proven by a separate gallery component integrated into `Main.qml`.
- Gallery uses canonical public Theme names and stable objectName anchors.
- qmlagent remains optional and no new third-party dependency is introduced.
</success_criteria>

<output>
Create `.planning/phases/05-verification-and-gallery/05-02-SUMMARY.md` when done.
</output>
