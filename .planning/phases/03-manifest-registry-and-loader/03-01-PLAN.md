---
phase: 03-manifest-registry-and-loader
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - CMakeLists.txt
  - tests/CMakeLists.txt
  - tests/Core/CMakeLists.txt
  - tests/Core/tst_merce_test_harness.cpp
autonomous: true
requirements:
  - MANIFEST-03
  - MANIFEST-04
  - MANIFEST-05
must_haves:
  truths:
    - "Main Merce repo has first-class automated QTest/CTest coverage before manifest loader behavior is implemented."
    - "Unknown registry entries, malformed JSON, strict validation, and fallback behavior can be proven by fast C++ tests in this phase."
    - "Qt Test is available only when BUILD_TESTING is enabled; normal Merce configure/build paths keep Node and Style Dictionary out of runtime per D-25."
  artifacts:
    - path: "tests/CMakeLists.txt"
      provides: "Main-repo test subtree entrypoint"
      contains: "add_subdirectory(Core)"
    - path: "tests/Core/CMakeLists.txt"
      provides: "Core QTest target helper"
      contains: "merce_add_qtest"
    - path: "tests/Core/tst_merce_test_harness.cpp"
      provides: "Concrete CTest/QTest sanity target"
      contains: "QTEST_MAIN"
  key_links:
    - from: "CMakeLists.txt"
      to: "tests/CMakeLists.txt"
      via: "BUILD_TESTING-gated add_subdirectory"
      pattern: "if\\(BUILD_TESTING\\)"
---

<objective>
Create the first-class main-repo test harness required before implementing the manifest registry and loader.

Purpose: Phase 3 validation cannot rely only on playground startup. MANIFEST-03, MANIFEST-04, and MANIFEST-05 require fast automated negative tests for rejection, validation, logging/fallback behavior, and default failure handling.
Output: CTest/QTest infrastructure for Merce Core tests, with Qt Test gated behind BUILD_TESTING and no new runtime dependency per D-25.
</objective>

<execution_context>
@/Users/techalp/.codex/get-shit-done/workflows/execute-plan.md
@/Users/techalp/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-manifest-registry-and-loader/03-CONTEXT.md
@.planning/phases/03-manifest-registry-and-loader/03-RESEARCH.md
@.planning/phases/03-manifest-registry-and-loader/03-PATTERNS.md
@.planning/phases/01-theme-runtime-contract/01-SUMMARY.md
@.planning/phases/02-token-build-pipeline/02-01-SUMMARY.md
@CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate Main-Repo Tests Behind BUILD_TESTING</name>
  <files>CMakeLists.txt</files>
  <read_first>
    - CMakeLists.txt
    - .planning/phases/03-manifest-registry-and-loader/03-RESEARCH.md
    - .planning/phases/03-manifest-registry-and-loader/03-PATTERNS.md
    - .planning/phases/02-token-build-pipeline/02-01-SUMMARY.md
  </read_first>
  <action>Add main-repo test activation without affecting normal consumer builds. Include CTest after Qt setup, and when BUILD_TESTING is enabled, require Qt6::Test and add_subdirectory(tests). Keep BUILD_TESTING as the switch that introduces Qt Test; do not add npm, Style Dictionary, or any package-manager action to the normal Qt configure/build path per D-25. Preserve the existing BUILD_MERCE_PLAYGROUND behavior and do not alter Merce module target order.</action>
  <verify>
    <automated>cmake -S . -B build -DBUILD_TESTING=ON -DBUILD_MERCE_PLAYGROUND=ON -DCMAKE_PREFIX_PATH=/Users/techalp/Qt/6.11.1/macos</automated>
  </verify>
  <acceptance_criteria>
    - CMakeLists.txt contains include(CTest), a BUILD_TESTING-gated Qt6::Test lookup, and add_subdirectory(tests).
    - cmake configure succeeds with BUILD_TESTING=ON using the existing Qt 6.11.1 prefix.
    - MERCE_ENABLE_TOKEN_BUILD remains default OFF and is not triggered by BUILD_TESTING.
  </acceptance_criteria>
  <done>Test infrastructure can be enabled from the main build without changing runtime dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Add Core QTest Harness Target</name>
  <files>tests/CMakeLists.txt, tests/Core/CMakeLists.txt, tests/Core/tst_merce_test_harness.cpp</files>
  <read_first>
    - tests/CMakeLists.txt
    - tests/Core/CMakeLists.txt
    - tests/Core/tst_merce_test_harness.cpp
    - external/qmlagent/tests/auto/qmlagent/CMakeLists.txt
    - external/qmlagent/tests/auto/qmlagent/tst_qmlagentprotocol.cpp
    - .planning/phases/03-manifest-registry-and-loader/03-PATTERNS.md
  </read_first>
  <action>Create tests/CMakeLists.txt and tests/Core/CMakeLists.txt. Define a small helper named merce_add_qtest(target source) that creates a Qt test executable, links Qt6::Core and Qt6::Test, and registers add_test with the same target name. Add tst_merce_test_harness.cpp as a concrete sanity target that proves the helper and CTest route work. Keep the file independent from the manifest loader so this wave can pass before loader symbols exist.</action>
  <verify>
    <automated>cmake --build build --target tst_merce_test_harness</automated>
    <automated>ctest --test-dir build -R tst_merce_test_harness --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - tests/Core/CMakeLists.txt contains merce_add_qtest and registers tst_merce_test_harness.
    - tst_merce_test_harness.cpp uses QTEST_MAIN and passes through ctest.
    - No loader source file is required for this target to compile.
  </acceptance_criteria>
  <done>Main-repo QTest/CTest infrastructure is present and verified.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- File: tests/CMakeLists.txt
- File: tests/Core/CMakeLists.txt
- File: tests/Core/tst_merce_test_harness.cpp
- CMake helper: merce_add_qtest(target source)
- Test target: tst_merce_test_harness
- CTest entry: tst_merce_test_harness
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build configuration -> test-only dependencies | BUILD_TESTING controls whether Qt6::Test is required. |
| automated tests -> loader behavior | Phase 3 negative cases must be executable, not manual-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | manifest loader validation coverage | mitigate | Add CTest/QTest route before loader work so unsafe paths, malformed JSON, schema confusion, and fallback behavior can be tested in later plans. |
| T-03-01-02 | Denial of Service | normal consumer build | mitigate | Gate Qt6::Test under BUILD_TESTING so normal Merce configure/build remains focused on runtime modules. |
| T-03-01-03 | Supply Chain | package-manager installs | accept | This plan introduces no npm/pip/cargo install and keeps MERCE_ENABLE_TOKEN_BUILD untouched per D-25. |
</threat_model>

<verification>
Run the automated commands in each task. After both tasks, `ctest --test-dir build -R tst_merce_test_harness --output-on-failure` must pass.
</verification>

<success_criteria>
- Main repo tests can be configured with BUILD_TESTING=ON.
- A Core QTest target builds and passes through CTest.
- No public QML API, manifest loader, or runtime theme behavior is changed by this plan.
</success_criteria>

<output>
Create `.planning/phases/03-manifest-registry-and-loader/03-01-SUMMARY.md` when done.
</output>
