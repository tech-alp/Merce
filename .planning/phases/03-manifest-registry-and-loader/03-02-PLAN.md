---
phase: 03-manifest-registry-and-loader
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/design-tokens/themes.json
  - tools/design-tokens/src/theme-registry.mjs
  - tools/design-tokens/build.mjs
  - tools/design-tokens/README.md
  - generated/themes/index.json
  - generated/themes/merce.default.json
  - generated/themes/merce.light.json
  - generated/themes/merce.dark.json
  - generated/themes/stripe.json
autonomous: true
requirements:
  - MANIFEST-01
  - MANIFEST-02
  - MANIFEST-04
must_haves:
  truths:
    - "generated/themes/index.json remains the generated source artifact and is packageable as :/merce/themes/index.json per D-01."
    - "Index entries include display names, defaults, variant paths, single-manifest paths, and optional basePath per D-02 and D-06."
    - "Merce default loading can resolve merce.default.json first and merce.light.json second without raw DTCG reference resolution per D-10, D-11, and D-12."
    - "Phase 3 manifest-backed apply is explicitly constrained to palette, spacing, radius, and typography; motion, iconography, zIndex, breakpoints, and shadows remain construction defaults per the D-14 support-section choice."
  artifacts:
    - path: "generated/themes/index.json"
      provides: "Authoritative generated theme registry"
      contains: "\"basePath\": \"merce.default.json\""
    - path: "generated/themes/merce.default.json"
      provides: "Resolved Merce base manifest for section-level overlay"
      contains: "\"theme\": \"merce\""
    - path: "tools/design-tokens/src/theme-registry.mjs"
      provides: "basePath-aware generated index and manifest entry generation"
      exports: ["themeEntries", "generatedIndex"]
  key_links:
    - from: "tools/design-tokens/themes.json"
      to: "generated/themes/index.json"
      via: "basePath emission"
      pattern: "basePath"
    - from: "generated/themes/index.json"
      to: "generated/themes/merce.default.json"
      via: "relative filename"
      pattern: "merce\\.default\\.json"
---

<objective>
Extend the generated registry and artifacts so the C++ loader can consume base-plus-active manifests through index.json.

Purpose: D-06 and D-10 require optional basePath support. D-11 through D-14 require resolved runtime manifests and section-level overlay semantics. This plan creates the generated artifacts the resource packaging and loader plans consume.
Output: basePath-aware generated index, generated merce.default.json, regenerated manifests, and documentation of the Phase 3 supported-section boundary.
</objective>

<execution_context>
@/Users/techalp/.codex/get-shit-done/workflows/execute-plan.md
@/Users/techalp/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-manifest-registry-and-loader/03-CONTEXT.md
@.planning/phases/03-manifest-registry-and-loader/03-RESEARCH.md
@.planning/phases/03-manifest-registry-and-loader/03-PATTERNS.md
@.planning/phases/02-token-build-pipeline/02-01-SUMMARY.md
@tools/design-tokens/themes.json
@tools/design-tokens/src/theme-registry.mjs
@tools/design-tokens/build.mjs
@tools/design-tokens/src/merce-manifest-format.mjs
@tools/design-tokens/src/validate-manifest.mjs
@generated/themes/index.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit basePath From The Generated Registry</name>
  <files>tools/design-tokens/themes.json, tools/design-tokens/src/theme-registry.mjs, tools/design-tokens/build.mjs</files>
  <read_first>
    - tools/design-tokens/themes.json
    - tools/design-tokens/src/theme-registry.mjs
    - tools/design-tokens/build.mjs
    - tools/design-tokens/src/validate-manifest.mjs
    - .planning/phases/03-manifest-registry-and-loader/03-CONTEXT.md
    - .planning/phases/03-manifest-registry-and-loader/03-PATTERNS.md
  </read_first>
  <action>Add optional per-theme basePath support. Set the Merce theme entry in tools/design-tokens/themes.json to basePath "merce.default.json" per D-06 and D-10. Update generatedIndex(registry) so variant-backed themes preserve displayName, basePath, defaultVariant, and variants. Update themeEntries(registry) so a theme with basePath produces a resolved base manifest entry using core sources plus theme.source, with no variant identity. Keep variant entries using core sources plus theme.source plus variantSources. Do not implement raw DTCG alias/reference resolution in C++; Style Dictionary remains the build-time resolver per D-11 and D-25.</action>
  <verify>
    <automated>cd tools/design-tokens && npm run build</automated>
    <automated>node tools/design-tokens/src/validate-manifest.mjs generated/themes/merce.default.json</automated>
  </verify>
  <acceptance_criteria>
    - tools/design-tokens/themes.json contains basePath "merce.default.json" under themes.merce.
    - generatedIndex emits basePath for merce without changing stripe.path semantics.
    - themeEntries emits a base manifest entry for merce with no variant field and keeps light/dark variant entries intact.
    - generated/themes/merce.default.json exists and validates with schemaVersion 1 and theme "merce".
  </acceptance_criteria>
  <done>Generated registry tooling supports optional base manifests without changing runtime dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate Artifacts And Document Supported Sections</name>
  <files>generated/themes/index.json, generated/themes/merce.default.json, generated/themes/merce.light.json, generated/themes/merce.dark.json, generated/themes/stripe.json, tools/design-tokens/README.md</files>
  <read_first>
    - generated/themes/index.json
    - generated/themes/merce.light.json
    - generated/themes/merce.dark.json
    - generated/themes/stripe.json
    - tools/design-tokens/README.md
    - .planning/phases/03-manifest-registry-and-loader/03-RESEARCH.md
    - .planning/phases/03-manifest-registry-and-loader/03-PATTERNS.md
  </read_first>
  <action>Regenerate the committed generated/themes artifacts after basePath support. Ensure index.json lists schemaVersion, defaultTheme, themes.merce.displayName, themes.merce.basePath, themes.merce.defaultVariant, themes.merce.variants.light, themes.merce.variants.dark, and themes.stripe.path per MANIFEST-02, D-01, and D-02. Add README wording that Phase 3 runtime apply validates and applies palette, spacing, radius, and typography from manifests while motion, iconography, zIndex, breakpoints, and shadows keep construction defaults; this resolves the D-14 support-section ambiguity without expanding the generated token taxonomy in this phase.</action>
  <verify>
    <automated>cd tools/design-tokens && npm run validate && npm run check</automated>
    <automated>test -f generated/themes/merce.default.json</automated>
    <automated>rg -n '"basePath": "merce.default.json"|"path": "stripe.json"' generated/themes/index.json</automated>
  </verify>
  <acceptance_criteria>
    - generated/themes/index.json contains basePath for merce and path for stripe.
    - generated/themes/merce.default.json has no variant field.
    - generated/themes/merce.light.json and generated/themes/merce.dark.json keep variant identities.
    - README states the Phase 3 supported-section boundary: palette, spacing, radius, typography manifest-backed; motion, iconography, zIndex, breakpoints, shadows construction defaults.
  </acceptance_criteria>
  <done>Committed generated artifacts and documentation are ready for Qt resource packaging.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- File: generated/themes/merce.default.json
- Updated generated artifact: generated/themes/index.json with themes.merce.basePath
- Updated function behavior: themeEntries(registry) emits base manifest entries
- Updated function behavior: generatedIndex(registry) preserves basePath
- Documented support-section boundary in tools/design-tokens/README.md
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| token tooling -> generated artifacts | Node tooling writes JSON consumed by C++ runtime resources. |
| generated index -> C++ loader | index.json becomes the authoritative path registry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | generated/themes/index.json | mitigate | Preserve schemaVersion, defaultTheme, displayName, basePath, path, and variants in generated output and verify with npm run validate/check. |
| T-03-02-02 | Tampering | base manifest overlay source | mitigate | Generate merce.default.json as a resolved runtime manifest; do not introduce C++ raw DTCG reference resolution per D-11. |
| T-03-02-03 | Information Disclosure | support-section source of truth | mitigate | Document the supported-section boundary so unsupported sections do not appear manifest-backed when they remain construction defaults. |
| T-03-02-04 | Supply Chain | package-manager installs | accept | No new npm package is installed; existing Phase 2 tooling is used only when regenerating artifacts and remains out of normal runtime per D-25. |
</threat_model>

<verification>
Run `cd tools/design-tokens && npm run build && npm run validate && npm run check`, then verify generated/themes/index.json contains basePath and generated/themes/merce.default.json exists.
</verification>

<success_criteria>
- MANIFEST-02 metadata is present in generated/themes/index.json.
- MANIFEST-01 resource packaging has all needed source files available under generated/themes.
- The Phase 3 supported-section choice is explicit and testable.
</success_criteria>

<output>
Create `.planning/phases/03-manifest-registry-and-loader/03-02-SUMMARY.md` when done.
</output>
