---
phase: 05-verification-and-gallery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - playground/ThemeProbe.qml
  - tests/Core/tst_merce_theme_manifest_loader.cpp
autonomous: true
requirements: [VERIFY-01, VERIFY-03]
user_setup: []
must_haves:
  truths:
    - "ThemeProbe verifies public C++ runtime values through canonical Theme.palette, Theme.iconography, Theme.breakpoints, spacing, radius, and typography."
    - "Manifest loader tests explicitly cover missing required fields, invalid schema version, unknown brand/mode, and fallback behavior."
    - "The verification hardening path stays deterministic and local with QtTest/CTest plus offscreen playground execution."
  artifacts:
    - path: "playground/ThemeProbe.qml"
      provides: "Canonical public theme value probe for VERIFY-01"
    - path: "tests/Core/tst_merce_theme_manifest_loader.cpp"
      provides: "Extended loader validation coverage for VERIFY-03"
  key_links:
    - from: "playground/ThemeProbe.qml"
      to: "Merce.Core/Theme"
      via: "canonical Theme singleton bindings"
      pattern: 'Theme\.palette|Theme\.iconography|Theme\.breakpoints'
    - from: "tests/Core/tst_merce_theme_manifest_loader.cpp"
      to: "MerceThemeManifestLoader"
      via: "QTemporaryDir manifest fixtures"
      pattern: 'MerceThemeManifestLoader\(.*\)\.load'
---

<objective>
Harden Phase 05 verification by making the focused QML theme probe canonical and closing the manifest loader test wording gaps for VERIFY-01 and VERIFY-03.

Purpose: ensure the runtime is observable through public Theme values and that loader validation failures are proven at the C++ boundary before gallery work builds on the same runtime.
Output: updated `ThemeProbe.qml` and additional assertions in `tst_merce_theme_manifest_loader.cpp`.
</objective>

<execution_context>
@/Users/techalp/.codex/get-shit-done/workflows/execute-plan.md
@/Users/techalp/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-verification-and-gallery/05-CONTEXT.md
@.planning/phases/05-verification-and-gallery/05-RESEARCH.md
@.planning/phases/05-verification-and-gallery/05-UI-SPEC.md
@.planning/phases/05-verification-and-gallery/05-PATTERNS.md
@.planning/phases/03-manifest-registry-and-loader/03-05-SUMMARY.md
@.planning/phases/04-runtime-brand-mode-switching/04-02-SUMMARY.md
@playground/ThemeProbe.qml
@playground/ThemeSwitchProbe.qml
@generated/themes/index.json
@generated/themes/merce.light.json
@generated/themes/merce.dark.json
@generated/themes/stripe.json
@Core/Theme/MerceTheme.h
@Core/Theme/MerceIconography.h
@Core/Theme/MerceBreakpoints.h
@tests/Core/tst_merce_theme_manifest_loader.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize ThemeProbe runtime assertions</name>
  <files>playground/ThemeProbe.qml</files>
  <read_first>
    - `.planning/phases/05-verification-and-gallery/05-CONTEXT.md` decisions D-01:, D-03:, D-04:, D-05:, D-06:.
    - `.planning/phases/05-verification-and-gallery/05-UI-SPEC.md` "Verification Checkpoints" and canonical API naming.
    - `playground/ThemeProbe.qml`
    - `playground/ThemeSwitchProbe.qml`
    - `generated/themes/merce.light.json`
    - `Core/Theme/MerceTheme.h`
    - `Core/Theme/MerceIconography.h`
    - `Core/Theme/MerceBreakpoints.h`
  </read_first>
  <action>
    Update `ThemeProbe.qml` so VERIFY-01 proves public runtime values through canonical names per D-04:, D-05:, and D-06:. Keep the route lightweight and deterministic per D-01: and D-03:. Replace compatibility alias checks such as `Theme.colors`, `Theme.icons`, and `Theme.breakpoint` with canonical checks for `Theme.palette.backgroundBase`, `Theme.palette.textPrimary`, `Theme.palette.actionPrimary`, `Theme.spacing.md`, `Theme.radius.button`, `Theme.typography.fontBody`, `Theme.iconography.small`, and `Theme.breakpoints.large`. Also assert the default active state is `Theme.activeBrand === "merce"` and `Theme.activeMode === "light"`. Use a `fail(message, values)` helper matching `ThemeSwitchProbe.qml` and keep successful output machine-checkable as `theme-probe ok` followed by canonical observed values only. Do not add a new runtime API, generated-token API, Spix, qmlagent dependency, or Qt Quick Controls style switching.
  </action>
  <acceptance_criteria>
    - `ThemeProbe.qml` contains no `Theme.colors`, `Theme.icons`, or `Theme.breakpoint` references.
    - `ThemeProbe.qml` asserts Merce light manifest-backed values `#faf8f6`, `#1f1510`, `#c4785a`, spacing `16`, radius `12`, and `DM Sans`.
    - `ThemeProbe.qml` asserts canonical construction-default support values through `Theme.iconography.small === 20` and `Theme.breakpoints.large === 1024`.
    - `MercePlayground --theme-probe` exits 0 offscreen and logs `theme-probe ok`.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build --target MercePlayground</automated>
    <automated>QT_QPA_PLATFORM=offscreen ./build/playground/MercePlayground --theme-probe</automated>
    <automated>rg -n "Theme\\.palette|Theme\\.iconography|Theme\\.breakpoints|theme-probe ok" playground/ThemeProbe.qml</automated>
    <automated>! rg -n "Theme\\.(colors|icons|breakpoint)\\b" playground/ThemeProbe.qml</automated>
  </verify>
  <done>VERIFY-01 is proven by a focused offscreen probe using only canonical public Theme names.</done>
</task>

<task type="auto">
  <name>Task 2: Close manifest loader validation wording gaps</name>
  <files>tests/Core/tst_merce_theme_manifest_loader.cpp</files>
  <read_first>
    - `.planning/phases/05-verification-and-gallery/05-CONTEXT.md` decisions D-10:, D-11:, D-12:.
    - `.planning/phases/05-verification-and-gallery/05-RESEARCH.md` "Manifest Validation Gaps To Audit".
    - `.planning/phases/05-verification-and-gallery/05-PATTERNS.md` `tests/Core/tst_merce_theme_manifest_loader.cpp` pattern.
    - `tests/Core/tst_merce_theme_manifest_loader.cpp`
    - `tests/Core/tst_merce_theme_runtime_switch.cpp`
    - `tests/Core/CMakeLists.txt`
  </read_first>
  <action>
    Extend `tests/Core/tst_merce_theme_manifest_loader.cpp` for VERIFY-03 per D-10: and D-11:, keeping the existing loader test file unless readability requires otherwise per D-12:. Add an explicit data-driven test slot for missing top-level required sections: remove each of `palette`, `spacing`, `radius`, and `typography` from `fullManifest(...)`, load through a temporary `index.json`, assert `!result.ok`, `!result.usedFallback`, and an error containing `missing required field: <section>`. Tighten existing invalid schema, unknown theme, unknown variant, and fallback tests with direct assertions if needed so the requirement wording is visibly represented in test names and expectations. Keep validation at the C++ loader/registry boundary; do not duplicate public runtime invalid behavior that is already covered by `tst_merce_theme_runtime_switch.cpp`.
  </action>
  <acceptance_criteria>
    - `tst_merce_theme_manifest_loader.cpp` has direct test coverage for missing `palette`, `spacing`, `radius`, and `typography` sections.
    - Existing invalid schema version, unknown brand, unknown mode, and fallback behavior assertions remain present and passing.
    - No new test target is created unless the existing file becomes unreadable; default path is extending `tst_merce_theme_manifest_loader.cpp`.
    - Loader validation uses temporary JSON fixtures and does not expose diagnostics through QML.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build --target tst_merce_theme_manifest_loader</automated>
    <automated>ctest --test-dir build -R tst_merce_theme_manifest_loader --output-on-failure</automated>
    <automated>rg -n "missingTopLevelRequiredSectionsAreRejected|schemaVersionMismatchIsRejected|unknownThemeIsRejected|unknownVariantIsRejected|requestedBadManifestFallsBackToRegistryDefault" tests/Core/tst_merce_theme_manifest_loader.cpp</automated>
  </verify>
  <done>VERIFY-03 maps cleanly to loader tests for missing fields, invalid schema version, unknown brand/mode, and fallback behavior.</done>
</task>

</tasks>

## Artifacts this phase produces

- Updated `playground/ThemeProbe.qml` canonical probe contract:
  - `theme-probe ok` output
  - canonical `Theme.palette`, `Theme.iconography`, and `Theme.breakpoints` assertions
- New or tightened private test slots in `tests/Core/tst_merce_theme_manifest_loader.cpp`:
  - `missingTopLevelRequiredSectionsAreRejected_data`
  - `missingTopLevelRequiredSectionsAreRejected`

<threat_model>
No high-severity threat is expected in this plan because it modifies local tests and an offscreen QML probe only. Mitigations remain explicit because untrusted manifest fixtures cross the loader boundary.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| temporary JSON fixture -> MerceThemeManifestLoader | Test-created manifest data is untrusted input to the loader. |
| QML probe -> Theme singleton | Probe reads public runtime values from the C++ singleton. |
| build/test process -> local workspace | Commands write only normal build/test outputs under the existing build tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | `tst_merce_theme_manifest_loader.cpp` fixtures | mitigate | Use `QTemporaryDir`, fixed fixture filenames, and existing path safety helpers; assert unsafe resource, absolute, slash, and parent paths remain rejected. |
| T-05-01-02 | Repudiation | probe/test failures | mitigate | Use terse deterministic failure messages with observed values so failed checks are attributable. |
| T-05-01-03 | Information Disclosure | `ThemeProbe.qml` output | accept | Probe prints only non-secret public token values and active brand/mode. |
| T-05-01-04 | Denial of Service | malformed manifest fixtures | mitigate | Keep malformed data inside QtTest temporary directories and assert loader rejects instead of hanging or falling through. |
| T-05-01-05 | Elevation of Privilege | new runtime APIs | mitigate | Do not add runtime API, diagnostics API, network hooks, Spix, or qmlagent dependency in this plan. |
| T-05-SC | Tampering | package/dependency surface | mitigate | No npm, pip, cargo, Spix, or external package install is planned. |
</threat_model>

<verification>
- `cmake --build build --target MercePlayground`
- `QT_QPA_PLATFORM=offscreen ./build/playground/MercePlayground --theme-probe`
- `cmake --build build --target tst_merce_theme_manifest_loader`
- `ctest --test-dir build -R tst_merce_theme_manifest_loader --output-on-failure`
- `ctest --test-dir build --output-on-failure`
- Source gate: `! rg -n "Theme\\.(colors|icons|breakpoint)\\b" playground/ThemeProbe.qml`
</verification>

<success_criteria>
- VERIFY-01 appears in this plan and is proven by `--theme-probe`.
- VERIFY-03 appears in this plan and is proven by focused CTest coverage.
- Probe and loader tests stay deterministic, local, and free of new third-party dependencies.
- Phase 05 scope exclusions stay out of the implementation: no Spix, no visual diff gate, no full component-state matrix.
</success_criteria>

<output>
Create `.planning/phases/05-verification-and-gallery/05-01-SUMMARY.md` when done.
</output>
